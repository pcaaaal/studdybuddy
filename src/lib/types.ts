export type StudyGroup = {
  id: string;
  name: string;
  subject: string;
  level: string;
  location: string;
  members: string[];
};

export type User = {
  id: string;
  name: string;
  school: string;
};
